# Implementation Control Prompt

Implement the unified capability registry described by `docs/proposal.md`, `docs/high-level-design.md`, and `docs/detailed-design.md`.

Work from `docs/tasks/progress.md`, keep each pass scoped, add focused tests, run the documented checks, inspect diffs, and record results. Preserve user changes and public compatibility. Ask before expanding to write tools, runtime registry mutation, or new infrastructure.
